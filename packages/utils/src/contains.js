/**
 * contains
 * 
 * checks haystack for needle
 * and returns boolean representing
 * if found or not
 * 
 * @param {any[]} haystack 
 * @param {any} needle 
 * @returns {boolean}
 */
export default (haystack, needle) => haystack.indexOf(needle) !== -1;
